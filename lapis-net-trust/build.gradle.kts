// lapis-net-trust: Veritas (web-of-trust) data model and scoring.
// Future waves (V0.1.5/V0.1.6) add the signed trust-edge model and the
// shortest-path aggregation algorithm here.

dependencies {
    implementation(project(":lapis-net-core"))
}
