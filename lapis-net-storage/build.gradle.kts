// lapis-net-storage: DHT / content-storage abstractions.
// Future waves (V0.1.4) add the Nabu (IPFS-on-libp2p) DHT + Bitswap integration here.

dependencies {
    implementation(project(":lapis-net-core"))
}
