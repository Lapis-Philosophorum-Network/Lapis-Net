// lapis-net-networking: libp2p node bootstrap and GossipSub propagation.
// Future waves (V0.1.3/V0.1.7) add the jvm-libp2p node setup, peer discovery
// and GossipSub-based record propagation here.

dependencies {
    implementation(project(":lapis-net-core"))
}
